{{- if .Values.tfsacalculation.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.tfsacalculation.applicationName }}
  namespace: {{ .Values.tfsacalculation.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.tfsacalculation.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.tfsacalculation.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.tfsacalculation.source.repoUrl }}
    targetRevision: {{ .Values.tfsacalculation.source.targetRevision }}
    path: {{ .Values.tfsacalculation.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.tfsacalculation.source.helm.values | nindent 8 }}
{{- end }}