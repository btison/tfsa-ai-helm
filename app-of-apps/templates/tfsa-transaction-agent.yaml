{{- if .Values.tfsatransaction.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.tfsatransaction.applicationName }}
  namespace: {{ .Values.tfsatransaction.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.tfsatransaction.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.tfsatransaction.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.tfsatransaction.source.repoUrl }}
    targetRevision: {{ .Values.tfsatransaction.source.targetRevision }}
    path: {{ .Values.tfsatransaction.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.tfsatransaction.source.helm.values | nindent 8 }}
{{- end }}