{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tfsa-calculation-agent.serviceAccountName" . }}
  labels:
    {{- include "tfsa-calculation-agent.labels" . | nindent 4 }}
  annotations:
    {{- include "tfsa-calculation-agent.argocd-syncwave" . | nindent 4 }}
{{- end }}
